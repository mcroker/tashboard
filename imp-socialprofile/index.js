/* == Imports == */
var Twitter = require('tashboard');

exports.handler = function(event, context) {
      tashboard.impsocial();
    },
    function(err){
      //console.log (output );
      context.fail();
    });

  };
};
